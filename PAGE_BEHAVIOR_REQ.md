this is how the pages should behave.
for both pdf and normal writings should not be these two seperate ways,
insted it should be a uniform way.
like in samsung notes. i open a new page/note(the entire note, i will call this note from now because inside this i need to have many pages to scroll)
and should be able to add pdfs, and when adding there should be an option to either add the pdf with the view, or just as an attachment.
there should be a way to type textx, or write as it supports now,
there should be some page size, so the default page size, then when i add a pdf as with the view( like the way pdfs are shown now) if i have some empty page before adding that,
then the pdf should start in  a new page below, like that i should be able to add pages to top/moddle of the pdf pages or at the bottom , (keep all theese options inside some well structured dropdown menu) and the other major thing is , i also should be able to add pages horizontaly aligned, as many as i want, so it gives that infinite canvas feel without being so un organized, and also can export to pdfs easily since we can merge horizontaly aligned pages into a one large landscape size page, and all other pages can remain same size.
adding orizontal pages should be as easy as scrolling aggresively to that size to add, like new page adding option in samsung note app.
i also should be able to copy / paste images, texts to the pages, resizee them as i wish
i should be able to select drawn shapes, with a lasso tool, which gives access to those resize options,
horizontal pages are just same size as its origin page, (the one we scroll on) and there should be options to select the background color, the dasignn(give few options)

when doing all these keep a clean design , options should be well organized,
remove that clear page button which is unnecesory.

when doing this, first think properly think of proper data structures we should use , since we need to add the pdf export feature as well. the data should be stred, viewd in a nice well structured manner
