<?xml version="1.0" encoding="utf-8"?>
<!-- Pre-Android-12 splash background (API 21+ variant — takes precedence
     over the base drawable/launch_background.xml on all real devices).
     @color/splash_background and @drawable/splash_logo each have
     values-night/drawable-night variants, so this one file automatically
     serves both light and dark. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
