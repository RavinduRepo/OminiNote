<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for Google Drive cloud sync (all build types, not just debug). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Voice recording over a canvas (the `record` plugin). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:label="omininote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="standard"
            android:taskAffinity=""
            android:resizeableActivity="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Open / import a .omninote notebook bundle. A .omninote is a ZIP;
                 file managers / chat apps hand out opaque content:// URIs (no
                 filename), reporting the type as application/zip or, for the
                 unknown extension, application/octet-stream — so we match those
                 types for direct "Open with". (A path-pattern filter can't work
                 when the URI has no filename.) The app appears for other
                 zip/unknown files too; the bundle is validated on import and a
                 wrong file is rejected with a message. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:mimeType="application/zip"/>
                <data android:mimeType="application/octet-stream"/>
            </intent-filter>
            <!-- Also match by filename when a URI does expose it (some pickers). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.omninote"/>
                <data android:scheme="file" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.omninote"/>
            </intent-filter>
            <!-- Share a bundle to the app (very reliable). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/zip"/>
                <data android:mimeType="application/octet-stream"/>
            </intent-filter>
            <!-- Open a PDF with the app ("Open with" / share): it lands in this
                 device's default target notebook as an annotatable canvas. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:mimeType="application/pdf"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/pdf"/>
            </intent-filter>
            <!-- omninote:// share links (Stage 3): tapping one opens the app and
                 imports the shared notebook. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="omninote"/>
            </intent-filter>
        </activity>
        <!-- Multi-window aux activities: each runs in its OWN process
             (android:process) so native libraries (pdfrx/pdfium FFI callbacks,
             which abort when invoked across isolates) don't clash between two
             Flutter engines in one process. Launched explicitly by
             MainActivity.openNewWindow; not exported, no intent-filters. -->
        <activity
            android:name=".MainActivityW1"
            android:process=":w1"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity=""
            android:resizeableActivity="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>
        <activity
            android:name=".MainActivityW2"
            android:process=":w2"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity=""
            android:resizeableActivity="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>
        <activity
            android:name=".MainActivityW3"
            android:process=":w3"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity=""
            android:resizeableActivity="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- super_clipboard: exposes copied images to other apps (clipboard
             writes on Android hand out content:// URIs, not raw bytes). -->
        <provider
            android:name="com.superlist.super_native_extensions.DataProvider"
            android:authorities="io.github.ravinduRepo.omininote.SuperClipboardDataProvider"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
